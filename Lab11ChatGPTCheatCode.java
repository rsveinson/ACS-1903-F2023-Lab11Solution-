import java.util.Scanner;
/** 
 * ACS-1903 Assignment X Question Y
 * @author 
*/

public class Lab11ChatGPTCheatCode{
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        

        System.out.println("end of program");
    }
/* Q3    
    @Override
    public boolean equals(Object obj) {
        if (this == obj) {
            return true;
        }

        if (obj == null || getClass() != obj.getClass()) {
            return false;
        }

        Shoe otherShoe = (Shoe) obj;
        return shoeID.equals(otherShoe.shoeID);
    }
*/

/* Q4
 *  @Override
    public boolean equals(Object obj) {
        if (this == obj) {
            return true;
        }

        if (obj == null || getClass() != obj.getClass()) {
            return false;
        }

        Horse otherHorse = (Horse) obj;
        return age == otherHorse.age && name.equals(otherHorse.name);
    }
 */
}
